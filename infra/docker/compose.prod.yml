version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: mongo_prod
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db

  api:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile.backend
    container_name: api_prod
    restart: unless-stopped
    env_file:
      - ../../.env.prod
    depends_on:
      - mongo

  nginx:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile.frontend
    environment:
      AUTH0_DOMAIN: dev-w2lewd1si042sauk.us.auth0.com
      AUTH0_CLIENT_ID: LATv8f4mWoSJeWttinR5tp1hFq1glxeL
      AUTH0_AUDIENCE: https://api.appointments-platform
      BASE_URL: https://letsmarter.com

    container_name: nginx_prod
    restart: unless-stopped
    env_file:
      - ../../.env.prod
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ../nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - certs:/etc/letsencrypt
      - certbot-www:/var/www/certbot
    depends_on:
      - api

  certbot:
    image: certbot/certbot
    container_name: certbot
    volumes:
      - certs:/etc/letsencrypt
      - certbot-www:/var/www/certbot
    entrypoint: >
      sh -c "certbot certonly --webroot -w /var/www/certbot
      -d letsmarter.com -d www.letsmarter.com
      --email tu-email@dominio.com --agree-tos --non-interactive --force-renewal"

volumes:
  mongo_data:
  certs:
  certbot-www:
